---
name: WhatsApp via GPT Maker
description: Integração WhatsApp via GPT Maker — disparo automático opcional por fonte, idempotência 24h, edge function send-whatsapp-gptmaker
type: feature
---

# Integração WhatsApp via GPT Maker

- **Endpoint upstream:** `POST https://api.gptmaker.ai/v2/channel/{channelId}/start-conversation`, Bearer token.
- **Body enviado:** `{ phone, message }` — `message` é mínima (`"."` por default, override via env `GPTMAKER_INITIAL_MESSAGE`). O agente do GPT Maker assume a conversa.
- **Secrets:** `GPTMAKER_TOKEN`, `GPTMAKER_CHANNEL_ID` (default), opcional `GPTMAKER_INITIAL_MESSAGE`.

## Fluxo
1. `ingest-lead` recebe lead novo.
2. Se `lead_sources.whatsapp_auto_send = true` → dispara `send-whatsapp-gptmaker` via `EdgeRuntime.waitUntil` (não bloqueia).
3. Função normaliza telefone para E.164 BR sem `+` (`5511999999999`).
4. Idempotência: bloqueia se já houve `whatsapp_send_log.status='sent'` para o lead nas últimas 24h.
5. Sucesso → log + `lead_activities` (`whatsapp_iniciado`) + `leads.whatsapp_sent = true`.

## Schema
- `lead_sources.whatsapp_auto_send` (bool) e `whatsapp_channel_id` (text, override opcional).
- `whatsapp_send_log`: id, lead_id, source_slug, phone, status (`sent|skipped_no_phone|skipped_duplicate|skipped_disabled|error`), gptmaker_response (jsonb), error, created_at. RLS admin SELECT only.

## UI
- `/admin/integracoes` → topo da lista mostra `WhatsAppPanel` com métricas 7d (enviados/erros/ignorados) e modal "Ver últimos envios".
- Card de cada fonte: badge "WhatsApp auto" quando ligado.
- `IntegrationFormDialog`: toggle "Disparar WhatsApp automático" + input opcional channel_id.

## Fora do escopo atual
- Botão de envio manual no Lead Drawer.
- Webhook reverso (mensagens do lead).
- Templates customizados (GPT Maker resolve a conversa).
